import Foundation

/// AI offense: unit production, attack targets, unit composition, assaults.
/// Stub; extend with target selection and coordination.
final class AIOffenseManager {
    func recommendAttackTarget(ownedUnits: [Entity], world: World, playerManager: PlayerManager) -> Entity? {
        return nil
    }
}
