import Foundation

/// AI defense: turret placement, walls, unit patrols, base priorities.
/// Stub; extend with threat assessment and placement logic.
final class AIDefenseManager {
    func recommendDefenseBuild(near position: Vector2, inventory: InventoryComponent, buildingRegistry: BuildingRegistry) -> (buildingId: String, position: IntVector2, direction: Direction)? {
        return nil
    }
}
